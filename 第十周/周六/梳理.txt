一.dom2级事件的兼容性处理
1this:通过包裹匿名函数
2顺序问题:自己模拟事件池,给不同事件添加不同的自定义属性数组,按照顺序存放事件;当事件触发时,用函数run找到对应的事件数组,然后按照顺序执行
e在绑定的函数中
通过call处理this
3.重复绑定
在on执行时,避免重复绑定
4.off:移除绑定时,直接splice会引起run执行时的数组塌陷,所以赋值为null
ps:因为null存在,所以在run执行时判断是不是一个函数
5.一个事件只能绑定一个run方法
6.run时,通过(ele["myEvent"+eventType])进行ie6-8的判断

二 拖拽
1.计算坐标
2.鼠标丢失:添加鼠标和盒子之间的联系 setCapture  chrome中绑定给document,此时注意this